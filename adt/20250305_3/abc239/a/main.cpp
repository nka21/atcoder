#include <bits/stdc++.h>
using namespace std;
using ll = long long;

int main()
{
    ll h;
    cin >> h;

    cout << fixed << setprecision(6) << endl;
    cout << sqrt(h * (12800000 + h)) << endl;
}
